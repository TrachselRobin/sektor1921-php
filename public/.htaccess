RewriteEngine On

# Existierende Dateien/Ordner direkt liefern
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sonst an index.php weiterreichen, Pfad in ?uri=
RewriteRule ^ index.php?uri=%{REQUEST_URI} [L,QSA]
